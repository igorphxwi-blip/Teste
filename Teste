local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- URL do arquivo da key no GitHub
local KeyURL = “https://raw.githubusercontent.com/igorphxwi-blip/Teste/refs/heads/main/key.txt”

-- Função que busca a key no GitHub
local function GetOnlineKey()
   local success, response = pcall(function()
      return game:HttpGet(KeyURL)
   end)
   if success and response then
      return response:gsub("%s+", "") -- remove espaços/linhas extras
   else
      return nil
   end
end

-- Aviso de vencimento / atualização
Rayfield:Notify({
   Title = "⚠️ Aviso Importante",
   Content = "A key pode mudar a qualquer momento. Verifique no Discord 🚨",
   Duration = 12,
   Image = 0,
})

-- Notificação de créditos
Rayfield:Notify({
   Title = "Liphyr LPX",
   Content = "tt:@igorphs",
   Duration = 6.5,
   Image = 0,
})

local Window = Rayfield:CreateWindow({
   Name = "Liphyr | LPX v0.0.1",
   Icon = 17868600705,
   LoadingTitle = "Liphyr/LPX",
   LoadingSubtitle = "by liphyr",
   ShowText = "LPX v0.0.1",
   Theme = "Light",
   ToggleUIKeybind = "Q",
   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false,
   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil,
      FileName = "CFGSaverLiphyrLPX"
   },
   Discord = {
      Enabled = false,
      Invite = "noinvitelink",
      RememberJoins = true
   },
   KeySystem = true,
   KeySettings = {
      Title = "Liphyr | LPX v0.0.1",
      Subtitle = "Key System",
      Note = "Peça a key no Discord/TT",
      FileName = "Key",
      SaveKey = true,
      GrabKeyFromSite = true, -- agora pega de site
      Key = {GetOnlineKey() or "INVALID"} -- pega key do GitHub
   }
})

-- Aba de Scripts
local Tab = Window:CreateTab("Scripts | Paid + Free", 0)

local Paragraph = Tab:CreateParagraph({Title = "Note", Content = "The hub is open-sourced so you can check the hub for loggers!"})

local Button = Tab:CreateButton({
   Name = "MirandaTween+",
   Callback = function()
      loadstring(game:HttpGet("https://pastefy.app/TnWWJ4NS/raw"))()
   end,
})

local Button = Tab:CreateButton({
   Name = "Auto Secret",
   Callback = function()
      loadstring(game:HttpGet("https://tcscripts.discloud.app/scripts/autosecret"))()
   end,
})

local Button = Tab:CreateButton({
   Name = "Spawner Brainrot",
   Callback = function()
      loadstring(game:HttpGet("https://gitlab.com/traxscriptss/traxscriptss/-/raw/main/visual2.lua"))()
   end,
})

local Button = Tab:CreateButton({
   Name = "Chilli Hub",
   Callback = function()
      loadstring(game:HttpGet("https://raw.githubusercontent.com/tienkhanh1/spicy/main/Chilli.lua"))()
   end,
})

local Button = Tab:CreateButton({
   Name = "Grow a Garden",
   Callback = function()
      loadstring(game:HttpGet("https://lunor.dev/loader"))()
   end,
})

local Button = Tab:CreateButton({
   Name = "99 Noites",
   Callback = function()
      loadstring(game:HttpGet("https://raw.githubusercontent.com/kedd063/KdmlScripts/refs/heads/main/99-Night-İn-The-Forest"))()
   end,
})

-- Aba de Configurações
local SetTab = Window:CreateTab("Settings", 0)

local Paragraph = SetTab:CreateParagraph({Title = "Note", Content = "Créditos: @liphyr"})

local Button = SetTab:CreateButton({
   Name = "Destroy Gui",
   Callback = function()
      Rayfield:Destroy()
   end,
})

local Button = SetTab:CreateButton({
   Name = "Copiar Discord",
   Callback = function()
       setclipboard("https://discord.gg/75PCbuNn")
       Rayfield:Notify({
           Title = "Copiado!",
           Content = "Link do Discord copiado para a área de transferência ✅",
           Duration = 5,
           Image = 0
       })
   end,
